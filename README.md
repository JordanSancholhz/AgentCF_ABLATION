# AgentCF: Ablation_Study

## Proxy Configuration
request.py是负责gpt系列的api, request1.py是负责glm系列的api(这里使用的模型是gpt-4o和glm-4.5) 要填写request.py的API_KEY 这是for gpt-4o

## bash 运行训练和测试
```bash
bash run_ablation_gating.sh
```

